<h1>Eat-Da-Burger!</h1>

<!-- need to add the devour button -->
<!-- sample code of the delete button -->
{{!-- <ul>
{{#each movies}}
    <li>
        <p>{{this.id}}. {{this.movie}}

        <form action="/delete_method=DELETE" method="POST">
        <input type="hidden" name="id" value="{{this.id}}">
        </form>
        </p>
    </li>
{{/each}}
</ul> --}}


<form method="POST" action="/api/burgers">
    <textarea type="text" name="burger"></textarea>
</form>

  <button type="submit">Submit</button>

  <h4>Enter a burger you want to eat Click the "Submit" button.</h4>
</form>
